<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.modelo.todolistapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".View.CreateSharedList"></activity>
        <activity
            android:name=".View.CreateListActivity"
            android:label="@string/title_activity_create_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.NavigationDrawerActivity"
            android:theme="@style/NoActionBar" android:screenOrientation="portrait"></activity>
        <activity android:name=".View.MainActivity" />
        <activity
            android:name=".View.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity

            >
        <activity
            android:name=".View.SignUpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">

        </activity>
    </application>

</manifest>